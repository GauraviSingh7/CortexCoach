{
  "name": "ai-coach-observer-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Real-time dashboard for the AI Coaching Observer",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "vite build --ssr scripts/ssr-smoke.tsx --outDir dist-ssr --logLevel error && node dist-ssr/ssr-smoke.js",
    "verify": "npm run typecheck && npm run smoke && npm run build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.13.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
